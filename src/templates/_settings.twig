{# @var plugin \wsydney76\ff\Plugin #}
{# @var settings \wsydney76\ff\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    label: 'Template Root',
    name: 'templateRoot',
    instructions: 'Enter a root relative to your templates directory, if you want to use your own templates.',
    value: settings.templateRoot
}) }}

{# Check min/max match settings rule #}
{{ forms.textField({
    type: 'number',
    label: 'Program Page Limit',
    name: 'programPageLimit',
    min: 2,
    max: 50,
    instructions: 'How many screenings should be displayed on the program page per load',
    value: settings.programPageLimit,
    errors: settings.errors('programPageLimit')
}) }}

<p>Disable the following sections if you do not use them.
    This will avoid unnecessary database queries and will make the site (a little) faster.</p>
<p>This will not remove the fields from the field layout. Do that manually. Existing content will not be touched.</p>

<div class="readable" style="margin-top: 24px;">
    <blockquote class="note warning">
        <p>Do not delete the fields themselves.</p>
    </blockquote>
</div>

{{ forms.lightswitchField({
    label: 'Enable Sections',
    name: 'enableSections',
    on: settings.enableSections
}) }}

{{ forms.lightswitchField({
    label: 'Enable Awards',
    name: 'enableAwards',
    on: settings.enableAwards
}) }}

{{ forms.lightswitchField({
    label: 'Enable Sponsors',
    name: 'enableSponsors',
    on: settings.enableSponsors
}) }}

{{ forms.lightswitchField({
    label: 'Enable Ads',
    name: 'enableAds',
    on: settings.enableAds
}) }}

{{ forms.lightswitchField({
    label: 'Enable Topics',
    name: 'enableTopics',
    on: settings.enableTopics
}) }}

{{ forms.lightswitchField({
    label: 'Enable Camera',
    name: 'enableCamera',
    on: settings.enableCamera
}) }}

{{ forms.lightswitchField({
    label: 'Enable Script',
    name: 'enableScript',
    on: settings.enableScript
}) }}

{{ forms.lightswitchField({
    label: 'Enable Genres',
    name: 'enableGenres',
    on: settings.enableGenres
}) }}


{{ forms.lightswitchField({
    label: 'Enable Countries',
    name: 'enableCountries',
    on: settings.enableCountries
}) }}
{{ forms.lightswitchField({
    label: 'Enable FilmPoster',
    name: 'enableFilmPoster',
    on: settings.enableFilmPoster
}) }}

{{ forms.lightswitchField({
    label: 'Enable Diaries',
    name: 'enableDiaries',
    on: settings.enableGenres
}) }}

{{ forms.lightswitchField({
    label: 'Enable Languages',
    name: 'enableLanguages',
    on: settings.enableLanguages
}) }}

{{ forms.lightswitchField({
    label: 'Enable Subtitle Languages',
    name: 'enableSubtitleLanguages',
    instructions: 'Ignored if languages are disabled',
    on: settings.enableSubtitleLanguages
}) }}

{{ forms.lightswitchField({
    label: 'Enable Credited With',
    name: 'enableCreditedWith',
    on: settings.enableCreditedWith
}) }}