{% include '@ff/_partials/sidebar-lists.twig' with {
    lists: [
        {heading: 'Location'|t('ff-light'), query: entry.locations, enabled: true},
        {heading: 'Film'|t('ff-light'), query: entry.films, enabled: true},
        {heading: 'People'|t('ff-light'), query: entry.people, enabled: true}
    ]
} only %}