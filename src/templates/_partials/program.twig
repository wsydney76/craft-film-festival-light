{% js craft.app.config.custom.htmxScriptUrl %}



{% embed '_layouts/embeds/content-xl.twig' with {showHeader: true, alignTitle: 'left'} %}
    {% block contentTop %}
        {% include '_layouts/partials/blocks.twig' with {
            query: entry.bodyContent
        } only %}


        <div class="mt-4">
            {{ sprig.component('@ff/_reactive/program.twig', {resetUrl: entry.url}) }}
        </div>
    {% endblock %}
{% endembed %}